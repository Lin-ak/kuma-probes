# ========== 节点信息 ==========
# 节点名称（会拼到 msg 里，方便辨识是哪台 VPS）
NODE_NAME="SG-1"

# ========== 通用参数 ==========
# 探测超时（秒）
TIMEOUT="10"

# 探针访问目标站点时的代理（没有就留空）
# 支持：http://127.0.0.1:7890  或  socks5h://127.0.0.1:1080
PROXY=""

# 推送到 Kuma 时的代理（一般留空；只有 Kuma 也必须走代理才填）
PUSH_PROXY=""

# 绑定网卡（例如 eth0；没有就留空）
NIC=""

# UA（可不改）
UA_BROWSER="Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36"

# ========== Push URL（每台机器不同：每个区域每个服务一个 token） ==========
KUMA_CLAUDE_PUSH="https://kuma.example.com/api/push/CLAUDE_TOKEN_FOR_THIS_REGION"
KUMA_CHATGPT_PUSH="https://kuma.example.com/api/push/CHATGPT_TOKEN_FOR_THIS_REGION"
KUMA_GEMINI_PUSH="https://kuma.example.com/api/push/GEMINI_TOKEN_FOR_THIS_REGION"
KUMA_TIKTOK_PUSH="https://kuma.example.com/api/push/TIKTOK_TOKEN_FOR_THIS_REGION"
KUMA_SUNLOGIN_PUSH="https://kuma.example.com/api/push/SUNLOGIN_TOKEN_FOR_THIS_REGION"

# ========== Sunlogin 域名 ==========
SUNLOGIN_HOST="sunlogin.oray.com.w.kunluncan.com"
